import { HiremeDirective } from './hireme.directive';

describe('HiremeDirective', () => {
  it('should create an instance', () => {
    const directive = new HiremeDirective();
    expect(directive).toBeTruthy();
  });
});
